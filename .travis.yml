language: python
python: "3.6"

stages:
#  - Test
  - Coverage

jobs:
  include:
#    - stage: Test
#      script: python -m unittest discover ./src --verbose

    - stage: Coverage
      install: pip install codecov
      script:
        - coverage help
        - coverage help run
        - coverage run -m src
      after_success: codecov